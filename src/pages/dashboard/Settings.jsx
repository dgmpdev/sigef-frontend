import { Box, Typography } from '@mui/material'

const Settings = () => (
  <Box>
    <Typography variant="h4" mb={2}>
      Paramètres
    </Typography>
    <Typography variant="body1" color="text.secondary">
      Paramétrage global de SIGEF.
    </Typography>
  </Box>
)

export default Settings

